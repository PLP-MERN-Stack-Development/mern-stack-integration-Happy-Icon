import React from "react";
import ReactDOM from "react-dom/client";

ReactDOM.createRoot(document.getElementById("root")).render(
  <h1 className="text-3xl text-center mt-20 text-blue-600">
    Hello from Vite + React!
  </h1>
);
